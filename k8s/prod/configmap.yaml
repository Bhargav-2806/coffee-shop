# Non-sensitive app configuration for Production — applied to kind-coffee-prod cluster
apiVersion: v1
kind: ConfigMap
metadata:
  name: coffee-shop-config
  namespace: coffee-prod
  labels:
    app: coffee-shop
    env: prod
data:
  NODE_ENV: "production"
  PORT: "8080"
