# Non-sensitive app configuration for QA — applied to kind-coffee-qa cluster
apiVersion: v1
kind: ConfigMap
metadata:
  name: coffee-shop-config
  namespace: coffee-qa
  labels:
    app: coffee-shop
    env: qa
data:
  NODE_ENV: "production"
  PORT: "8080"
